{
  "name": "payment-api",
  "version": "1.0.0",
  "description": "handles billing/checkouts via stripe",
  "private": true,
  "main": ".output/server.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "rimraf .output && tsc -p tsconfig.json",
    "start": "node .output/server.js"
  },
  "dependencies": {
    "fastify": "^5.6.0",
    "chalk": "^5.6.2"
  },
  "devDependencies": {
    "@safeoutput/eslint-config": "workspace:*",
    "@safeoutput/typescript-config": "workspace:*",
    "typescript": "~5.9.2",
    "tsx": "^4.20.5",
    "@types/node": "^24.5.2",
    "rimraf": "^6.0.1"
  }
}
